import type { UserPagesConfig } from '@uni-helper/uni-pages-types'

/**
 * 定义 uni-pages 配置
 * 在 pages.config.ts 配置文件中使用，提供类型提示与校验
 *
 * @param config - 页面配置对象
 * @returns 配置对象
 */
export function defineUniPages(config: UserPagesConfig): UserPagesConfig {
  return config
}
